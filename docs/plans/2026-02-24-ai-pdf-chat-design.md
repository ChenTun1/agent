# AI PDF Chat - 智能PDF对话工具 设计文档

**创建日期**: 2026-02-24
**项目类型**: 个人开发者产品 + 可盈利SaaS
**开发者**: 实习生独立项目
**预计开发周期**: 3周MVP + 3周优化
**目标**: 学习项目 + 可发布产品 + 月收入$100-500

---

## 一、项目概述

### 1.1 产品定位

**产品名称**: AI PDF Chat (中文名:智能PDF对话)
**一句话描述**: 上传PDF,用自然语言提问,AI精准回答并标注出处
**Slogan**: "Stop reading, start asking"

**核心价值主张**:
- 📄 **解放阅读负担**: 2小时阅读 → 10分钟问答
- 🎯 **精准定位信息**: 不用Ctrl+F,直接问AI
- 📌 **可信赖答案**: 每个回答都标注来源页码
- ⚡ **即时理解**: 上传即用,无需学习曲线

### 1.2 目标用户

**主要用户群**(按优先级):

1. **学生/研究者** (40%市场)
   - 需求: 快速阅读学术论文、教材
   - 痛点: 论文太长(30-50页),专业术语多
   - 付费意愿: $10-15/月
   - 使用频率: 每周10+ PDF

2. **职场白领** (35%市场)
   - 需求: 理解合同、报告、技术文档
   - 痛点: 文档复杂,担心漏掉关键信息
   - 付费意愿: $15-20/月
   - 使用频率: 每周5+ PDF

3. **开发者/技术人员** (15%市场)
   - 需求: 查阅API文档、技术手册
   - 痛点: 文档长,搜索不准确
   - 付费意愿: $15-20/月
   - 使用频率: 每周5-10 PDF

4. **其他专业人士** (10%市场)
   - 咨询师、律师助理等
   - 偶尔使用,但愿意付费

### 1.3 市场验证

**已验证的成功案例**(2026真实数据):

| 产品 | 月收入 | 定价 | 备注 |
|------|--------|------|------|
| **PDF.ai** | $60K+ MRR | $20/月 | 标杆产品 |
| **ChatPDF** | 未公开 | $15/月 | 竞品 |
| **Humata** | 未公开 | $15/月 | 面向企业 |

**市场趋势**:
- AI工具付费用户增长300%+ (2024-2026)
- 用户愿意为AI工具付$10-20/月 (来源: 2026市场调研)
- PDF处理是最常见的AI应用场景之一

**我的机会**:
- ✅ 市场已验证,不是伪需求
- ✅ 竞品定价高($15-20/月),有降价空间
- ✅ 中文市场竞争少
- ✅ 可以差异化(更好的准确率、更低价格)

---

## 二、产品功能设计

### 2.1 MVP功能范围(3周完成)

**核心功能**(必须有):

✅ **PDF上传与管理**
- 拖拽上传PDF文件
- 支持最大10MB文件
- 自动提取文本内容
- 显示文档基本信息(页数、大小)

✅ **智能问答**
- 用户输入自然语言问题
- AI理解问题意图
- 从PDF中检索相关内容
- 生成准确回答
- **强制标注来源页码**

✅ **来源追溯**
- 每个回答显示来源页码
- 可点击跳转到原文查看
- 高亮显示相关段落

✅ **对话历史**
- 保存问答记录
- 可查看历史对话
- 可继续追问

✅ **基础用户系统**
- 匿名使用(无需注册)
- 可选注册(保存历史)
- 简单的使用限制(免费版)

**暂不包含**(后期扩展):

❌ OCR扫描件识别(技术复杂)
❌ 多PDF同时对话(增加复杂度)
❌ 导出对话记录(非核心)
❌ 团队协作功能(不是MVP重点)
❌ 移动App(先做Web版)
❌ 高级图表理解(需要GPT-4V,成本高)

### 2.2 用户使用流程

```
[用户访问网站]
    ↓
[首页简介 + 拖拽上传区域]
    ↓
[上传PDF文件]
    ↓
[后台处理: 提取文本 → 分块 → 向量化]
    ↓
[显示"已完成,可以开始提问"]
    ↓
[用户在聊天框输入问题]
    ↓
[AI检索 → 生成回答 → 标注来源]
    ↓
[显示回答,可点击页码查看原文]
    ↓
[用户继续提问或上传新PDF]
```

### 2.3 核心交互设计

#### 首页
```
┌─────────────────────────────────────┐
│   AI PDF Chat                       │
│   Stop reading, start asking        │
├─────────────────────────────────────┤
│                                     │
│   📄 拖拽PDF文件到这里上传          │
│   或 [点击选择文件]                 │
│                                     │
│   支持最大10MB,仅限文字PDF         │
│                                     │
│   ──────────────────────            │
│                                     │
│   ✨ 特点:                          │
│   • 精准回答,标注来源页码          │
│   • 2小时阅读 → 10分钟理解          │
│   • 免费试用,无需注册              │
└─────────────────────────────────────┘
```

#### 对话界面
```
┌─────────────────────────────────────┐
│  📄 research-paper.pdf (50页)  [x] │
├─────────────────────────────────────┤
│                                     │
│  💡 推荐问题:                        │
│  • 这篇文档的主要内容是什么?        │
│  • 总结关键发现                     │
│  • 实验方法是什么?                  │
│                                     │
│  ──────────────────────            │
│                                     │
│  You: 论文的主要结论是什么?         │
│                                     │
│  AI: 根据第47-48页的结论部分,      │
│      这篇论文的主要发现是...        │
│      [来源: 第47页, 第48页]         │
│      [📍 查看原文]                  │
│                                     │
│  You: 使用了什么数据集?              │
│                                     │
│  AI: 论文在第12页明确提到使用了     │
│      ImageNet数据集,包含...         │
│      [来源: 第12页]                 │
│      [📍 查看原文]                  │
│                                     │
├─────────────────────────────────────┤
│  💬 输入你的问题...         [发送] │
└─────────────────────────────────────┘
```

### 2.4 智能推荐问题

**初次上传PDF后,自动生成推荐问题**:
```python
def generate_suggested_questions(pdf_content):
    """基于PDF内容生成3-5个推荐问题"""

    # 分析文档类型
    doc_type = detect_document_type(pdf_content)

    if doc_type == "academic_paper":
        return [
            "这篇论文的主要贡献是什么?",
            "使用了什么研究方法?",
            "实验结果如何?",
            "有哪些局限性?"
        ]
    elif doc_type == "contract":
        return [
            "合同的主要条款是什么?",
            "违约责任如何规定?",
            "支付条件是什么?",
            "合同期限多久?"
        ]
    elif doc_type == "technical_doc":
        return [
            "这个工具如何使用?",
            "有哪些主要功能?",
            "如何安装配置?",
            "常见问题有哪些?"
        ]
    else:
        return [
            "这份文档的主要内容是什么?",
            "有哪些关键信息?",
            "总结全文要点"
        ]
```

---

## 三、技术架构设计

### 3.1 技术栈选型

#### 前端
```
Next.js 14 (App Router)
├── TypeScript
├── Tailwind CSS
├── shadcn/ui (组件库)
└── React Query (数据获取)

或者更简单的选择(MVP阶段):
Streamlit (Python全栈)
├── 优势: 快速开发,一个文件搞定
├── 劣势: 定制化有限
└── 适合: 快速验证MVP
```

**推荐**: MVP先用Streamlit,验证后再用Next.js重写

#### 后端
```
Python FastAPI
├── PyPDF2 (PDF文本提取)
├── LangChain (RAG框架)
├── Anthropic Claude API (主要LLM)
├── OpenAI Embeddings (文本向量化)
└── Pydantic (数据验证)
```

#### 数据库
```
PostgreSQL (Supabase托管)
├── 用户信息
├── PDF元数据
└── 对话历史

Qdrant (向量数据库)
├── 本地Docker部署
└── 存储文档chunks的embeddings
```

#### 部署
```
前端: Vercel (免费)
后端: Railway / Fly.io ($5-20/月)
向量DB: Qdrant Cloud (免费1GB)
数据库: Supabase (免费500MB)
```

### 3.2 系统架构图

```
┌─────────────────────────────────────────┐
│         Browser (用户)                  │
│  ┌─────────────────────────────────┐   │
│  │  Next.js / Streamlit            │   │
│  │  - 上传界面                      │   │
│  │  - 聊天界面                      │   │
│  │  - PDF查看器                     │   │
│  └─────────────────────────────────┘   │
└──────────────┬──────────────────────────┘
               │ HTTPS/WebSocket
┌──────────────▼──────────────────────────┐
│        FastAPI 后端                     │
│  ┌─────────────────────────────────┐   │
│  │  API Endpoints                  │   │
│  │  ├─ POST /upload                │   │
│  │  ├─ POST /chat                  │   │
│  │  ├─ GET  /history               │   │
│  │  └─ GET  /pdf/{id}/page/{num}  │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  核心处理模块                    │   │
│  │                                 │   │
│  │  PDF处理:                        │   │
│  │  ├─ PyPDF2提取文本              │   │
│  │  ├─ 智能分块                     │   │
│  │  └─ 页码追踪                     │   │
│  │                                 │   │
│  │  RAG系统:                        │   │
│  │  ├─ 混合检索(语义+关键词)       │   │
│  │  ├─ 重排序(Rerank)              │   │
│  │  └─ 上下文构建                   │   │
│  │                                 │   │
│  │  LLM调用:                        │   │
│  │  ├─ Prompt工程                  │   │
│  │  ├─ 强制引用来源                │   │
│  │  └─ 答案验证                     │   │
│  └─────────────────────────────────┘   │
└──────┬────────────┬─────────────────────┘
       │            │
┌──────▼────┐  ┌───▼──────────┐
│  Claude   │  │   Qdrant     │
│    API    │  │  Vector DB   │
│           │  │              │
│ - Sonnet  │  │ Collections: │
│ - Haiku   │  │ - pdf_chunks │
└───────────┘  └──────────────┘
       │            │
┌──────▼────────────▼───────┐
│   PostgreSQL (Supabase)   │
│   ┌───────────────────┐   │
│   │ Tables:           │   │
│   │ - users           │   │
│   │ - pdfs            │   │
│   │ - conversations   │   │
│   │ - messages        │   │
│   └───────────────────┘   │
└───────────────────────────┘
```

### 3.3 数据库设计

#### PostgreSQL Schema

```sql
-- 用户表
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE,
    created_at TIMESTAMP DEFAULT NOW(),
    subscription_tier VARCHAR(20) DEFAULT 'free', -- free, pro
    is_anonymous BOOLEAN DEFAULT true
);

-- PDF文档表
CREATE TABLE pdfs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    filename VARCHAR(255) NOT NULL,
    file_size INTEGER,
    page_count INTEGER,
    upload_date TIMESTAMP DEFAULT NOW(),
    status VARCHAR(20), -- processing, ready, error
    storage_path TEXT,

    INDEX idx_user_pdfs (user_id, upload_date DESC)
);

-- 对话表
CREATE TABLE conversations (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    pdf_id UUID REFERENCES pdfs(id) ON DELETE CASCADE,
    user_id UUID REFERENCES users(id),
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- 消息表
CREATE TABLE messages (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    conversation_id UUID REFERENCES conversations(id) ON DELETE CASCADE,
    role VARCHAR(20) CHECK (role IN ('user', 'assistant')),
    content TEXT NOT NULL,
    cited_pages INTEGER[], -- 引用的页码数组
    created_at TIMESTAMP DEFAULT NOW(),

    -- 用于分析和优化
    user_feedback VARCHAR(20), -- helpful, inaccurate, null
    retrieved_chunks_ids TEXT[], -- 检索到的chunk IDs

    INDEX idx_conversation_messages (conversation_id, created_at)
);
```

#### Qdrant Collection Schema

```python
# Collection: pdf_chunks
{
    "vectors": {
        "size": 1536,  # OpenAI text-embedding-3-small
        "distance": "Cosine"
    },
    "payload_schema": {
        "pdf_id": "keyword",      # 关联的PDF ID
        "page_num": "integer",    # 页码
        "chunk_id": "keyword",    # chunk唯一ID
        "text": "text",           # 原始文本
        "chunk_type": "keyword",  # paragraph, table, list
        "char_count": "integer"   # 字符数
    }
}
```

### 3.4 核心算法设计

#### 3.4.1 智能PDF分块算法

```python
def smart_chunking(pdf_path):
    """智能分块策略 - 保持语义完整性"""

    # 1. 提取PDF文本(按页)
    pages = extract_pages_with_pypdf2(pdf_path)

    chunks = []

    for page_num, page_text in enumerate(pages, start=1):
        # 2. 检测并处理表格
        tables = detect_tables(page_text)
        for table in tables:
            chunks.append({
                'text': format_table_as_text(table),
                'page': page_num,
                'type': 'table',
                'metadata': {'table_index': len(chunks)}
            })
            # 从页面文本中移除表格
            page_text = remove_table_from_text(page_text, table)

        # 3. 段落分割
        paragraphs = split_into_paragraphs(page_text)

        for para in paragraphs:
            # 4. 长段落按句子分割(保持重叠)
            if len(para) > 1000:  # 字符数阈值
                sentences = split_into_sentences(para)

                # 滑动窗口:5句话一组,重叠2句
                for i in range(0, len(sentences), 3):
                    chunk_text = ' '.join(sentences[i:i+5])
                    if chunk_text.strip():
                        chunks.append({
                            'text': chunk_text,
                            'page': page_num,
                            'type': 'paragraph',
                            'metadata': {
                                'is_partial': i + 5 < len(sentences)
                            }
                        })
            else:
                # 短段落直接作为chunk
                chunks.append({
                    'text': para,
                    'page': page_num,
                    'type': 'paragraph'
                })

        # 5. 检测列表
        lists = detect_lists(page_text)
        for lst in lists:
            chunks.append({
                'text': format_list_as_text(lst),
                'page': page_num,
                'type': 'list'
            })

    return chunks
```

**分块原则**:
- ✅ 保持段落完整性
- ✅ 表格单独处理
- ✅ 长段落有重叠(避免截断)
- ✅ 保留页码信息

#### 3.4.2 混合检索算法

```python
async def retrieve_relevant_chunks(question, pdf_id, k=5):
    """混合检索策略 - 提高准确率"""

    # 策略1: 向量语义搜索
    question_embedding = await get_embedding(question)
    semantic_results = qdrant_client.search(
        collection_name="pdf_chunks",
        query_vector=question_embedding,
        query_filter={
            "must": [{"key": "pdf_id", "match": {"value": pdf_id}}]
        },
        limit=10
    )

    # 策略2: 关键词BM25搜索
    keywords = extract_keywords(question)
    bm25_results = bm25_search(keywords, pdf_id, limit=10)

    # 策略3: 页码直接匹配
    page_mention = extract_page_number(question)
    direct_results = []
    if page_mention:
        direct_results = get_chunks_by_page(pdf_id, page_mention)

    # 合并结果
    all_chunks = merge_results([
        semantic_results,
        bm25_results,
        direct_results
    ])

    # 重排序(使用LLM)
    if len(all_chunks) > k:
        reranked = await rerank_with_claude(question, all_chunks, top_k=k)
        return reranked

    return all_chunks[:k]
```

**检索优化**:
- ✅ 多策略融合
- ✅ LLM重排序
- ✅ 页码直接匹配优先

#### 3.4.3 强制引用来源的Prompt

```python
SYSTEM_PROMPT = """你是一个专业的PDF文档助手。

严格规则(必须遵守):
1. 只回答PDF文档中明确提到的内容
2. 每个回答必须标注来源页码,格式:[来源: 第X页]
3. 如果文档中没有相关信息,明确说"文档中未找到相关内容"
4. 绝对不要编造、推测或使用文档外的知识
5. 如果多个页面都提到相关内容,列出所有页码

回答格式示例:
根据第23页的内容,实验的准确率达到95.3%,对比基线模型提升了12个百分点。[来源: 第23页]

或者多来源:
论文在第12页介绍了数据集,在第15页描述了预处理方法...[来源: 第12页, 第15页]

记住:准确性 > 完整性。宁可说"未找到",也不要猜测!
"""

def build_prompt(question, retrieved_chunks):
    """构建最终Prompt"""

    # 整理检索到的内容
    context = ""
    for idx, chunk in enumerate(retrieved_chunks, 1):
        context += f"\n[段落{idx} - 第{chunk['page']}页]\n"
        context += chunk['text']
        context += "\n---\n"

    user_prompt = f"""
基于以下PDF文档内容回答问题:

{context}

用户问题: {question}

请严格按照系统规则回答,必须标注来源页码!
"""

    return user_prompt
```

#### 3.4.4 答案验证机制

```python
async def validate_answer(answer, retrieved_chunks):
    """验证AI回答的质量"""

    validation_results = {
        'has_citation': False,
        'valid_pages': False,
        'consistent_with_context': False,
        'confidence': 0.0
    }

    # 检查1: 是否包含来源标注
    if "来源" in answer and ("第" in answer or "页" in answer):
        validation_results['has_citation'] = True
    else:
        return validation_results  # 直接失败

    # 检查2: 提取并验证页码
    cited_pages = extract_page_numbers(answer)
    chunk_pages = [c['page'] for c in retrieved_chunks]

    if all(page in chunk_pages for page in cited_pages):
        validation_results['valid_pages'] = True

    # 检查3: 内容一致性(使用小模型快速检查)
    consistency_prompt = f"""
    检索到的内容: {retrieved_chunks[0]['text'][:500]}
    AI的回答: {answer}

    问题:AI的回答是否基于检索到的内容?回答Yes或No
    """

    consistency_check = await claude_haiku_call(consistency_prompt)
    if "yes" in consistency_check.lower():
        validation_results['consistent_with_context'] = True

    # 计算总体置信度
    score = sum([
        validation_results['has_citation'] * 0.3,
        validation_results['valid_pages'] * 0.3,
        validation_results['consistent_with_context'] * 0.4
    ])
    validation_results['confidence'] = score

    return validation_results
```

---

## 四、质量保证策略

### 4.1 准确率目标

| 指标 | MVP目标 | 优化后目标 |
|------|---------|-----------|
| 页码引用准确率 | >90% | >95% |
| 内容准确率 | >80% | >90% |
| 零幻觉率 | 100% | 100% |
| 响应时间 | <5秒 | <3秒 |

### 4.2 测试策略

#### 自动化测试集

```python
# tests/golden_qa_set.json
{
    "academic_paper.pdf": [
        {
            "question": "论文的主要贡献是什么?",
            "expected_pages": [1, 47],
            "expected_keywords": ["贡献", "提出", "方法"]
        },
        {
            "question": "实验准确率是多少?",
            "expected_pages": [23, 28],
            "expected_answer_contains": ["95", "准确率", "%"]
        }
    ],
    "contract.pdf": [
        {
            "question": "违约金是多少?",
            "expected_pages": [8],
            "expected_answer_contains": ["违约", "月租金", "3"]
        }
    ]
}
```

**每次代码更新后运行测试**:
```bash
pytest tests/test_accuracy.py --golden-set
# 确保准确率不下降
```

#### 人工测试流程

**发布前检查清单**:
- [ ] 准备10个不同类型PDF(论文、合同、手册、报告)
- [ ] 每个PDF问5-10个问题
- [ ] 检查所有回答:
  - 是否有页码引用?
  - 引用是否正确?
  - 有没有编造内容?
- [ ] 目标:50个问题中至少45个完全正确

### 4.3 用户反馈收集

**界面中加入反馈按钮**:
```
AI回答后显示:
┌─────────────────────────────┐
│ 这个回答有帮助吗?            │
│ 👍 有帮助  👎 不准确  📝 反馈 │
└─────────────────────────────┘
```

**数据驱动优化**:
- 收集所有负面反馈
- 每周分析top 10问题
- 针对性优化Prompt或检索策略

---

## 五、商业模式设计

### 5.1 定价策略

**Freemium模式**:

```
免费版 (Free):
├─ 每天3个PDF
├─ 每个PDF 10个问题
├─ 文件大小限制: 5MB
├─ 保存7天
└─ 有广告(可选)

专业版 (Pro): $12/月 或 $99/年
├─ 每天无限PDF
├─ 每个PDF无限问题
├─ 文件大小限制: 50MB
├─ 永久保存
├─ 无广告
├─ 优先处理速度
└─ 邮件支持

高级版 (Premium): $29/月 或 $249/年
├─ Pro版所有功能
├─ OCR支持(扫描件)
├─ 批量上传
├─ API接入
├─ 优先客服
└─ 高级分析功能
```

**定价理由**:
- 竞品价格$15-20/月,我们$12/月有竞争力
- 年付折扣17%,鼓励长期订阅
- 免费版足够试用,但有限制引导付费

### 5.2 成本分析

**单用户成本**(Pro用户,每月使用20个PDF,每个10个问题):

```
AI API成本:
- Embedding (OpenAI): 20 PDF × 平均1万tokens × $0.00002 = $0.004
- Claude Sonnet调用: 200问答 × 平均2K tokens × $0.003 = $1.20
- 总AI成本: ~$1.20/月/用户

基础设施成本(分摊):
- 数据库(Supabase): $25/月 ÷ 预估100用户 = $0.25/用户
- 向量DB(Qdrant): $0 (免费层1GB够用)
- 服务器(Railway): $20/月 ÷ 100用户 = $0.20/用户
- 总基础成本: ~$0.45/月/用户

总成本: ~$1.65/月/Pro用户
毛利: $12 - $1.65 = $10.35 (86%毛利率!)
```

**扩展成本**(1000用户,10%付费率):

```
月度总成本:
- 900免费用户 × $0.10 = $90
- 100付费用户 × $1.65 = $165
- 基础设施: $100 (升级套餐)
- 总成本: ~$355/月

月度收入:
- 100付费用户 × $12 = $1200/月

净利润: $1200 - $355 = $845/月 (70%利润率)
```

### 5.3 增长预测

**保守估计**(6个月):

| 月份 | 总用户 | 付费用户 | MRR | 成本 | 净利润 |
|------|--------|----------|-----|------|--------|
| 1 | 50 | 2 | $24 | $50 | -$26 |
| 2 | 150 | 7 | $84 | $80 | $4 |
| 3 | 300 | 15 | $180 | $120 | $60 |
| 4 | 500 | 25 | $300 | $180 | $120 |
| 5 | 800 | 40 | $480 | $250 | $230 |
| 6 | 1200 | 60 | $720 | $350 | $370 |

**关键假设**:
- 付费转化率: 5% (保守)
- 月增长率: 50-80% (Reddit+Product Hunt推广)
- 留存率: D30 >25%

**乐观场景**(如果Product Hunt成功):
- 第一个月可能获得500-1000用户
- 6个月达到$1500-2000 MRR

---

## 六、开发计划

### 6.1 时间线(6周)

#### Week 1-2: MVP开发(Streamlit版)

**目标**: 做出可用的原型

**Day 1-3**: 核心功能
- [ ] PDF上传和文本提取
- [ ] 基础分块算法
- [ ] Qdrant本地部署

**Day 4-7**: RAG系统
- [ ] 向量化和存储
- [ ] 基础检索
- [ ] Claude API集成

**Day 8-10**: 问答优化
- [ ] Prompt工程
- [ ] 强制引用来源
- [ ] 简单界面

**Day 11-14**: 测试和修复
- [ ] 准备测试PDF
- [ ] 自测50个问题
- [ ] 修复bug

**交付物**: Streamlit演示版,本地运行

---

#### Week 3-4: 优化和完善

**目标**: 提高准确率,优化体验

**Day 15-18**: 检索优化
- [ ] 实现混合检索
- [ ] 添加BM25
- [ ] Rerank机制

**Day 19-21**: 质量提升
- [ ] 答案验证
- [ ] 智能推荐问题
- [ ] 错误处理

**Day 22-25**: 用户系统
- [ ] 匿名使用
- [ ] 简单注册
- [ ] 使用限制

**Day 26-28**: 小范围测试
- [ ] 邀请5-10个朋友
- [ ] 收集反馈
- [ ] 迭代优化

**交付物**: 功能完整的MVP,可在线访问

---

#### Week 5-6: 发布准备

**Day 29-32**: 部署上线
- [ ] 部署到Railway
- [ ] 配置域名
- [ ] 性能优化
- [ ] 监控告警

**Day 33-35**: 付费集成
- [ ] Stripe集成
- [ ] 订阅管理
- [ ] 使用限额控制

**Day 36-38**: 营销准备
- [ ] 制作演示视频
- [ ] 准备Product Hunt文案
- [ ] Reddit发布策略
- [ ] 落地页优化

**Day 39-42**: 正式发布
- [ ] Product Hunt发布
- [ ] Reddit r/SideProject发布
- [ ] 个人社交媒体
- [ ] 收集初期反馈

**交付物**: 正式产品,可付费使用

---

### 6.2 里程碑验收标准

**Milestone 1** (Week 2结束):
- [ ] 能上传PDF并提问
- [ ] AI能回答并标注页码
- [ ] 准确率>70% (自测10个PDF)

**Milestone 2** (Week 4结束):
- [ ] 准确率>85%
- [ ] 5个朋友试用,反馈"有用"
- [ ] 零严重bug

**Milestone 3** (Week 6结束):
- [ ] 产品上线,可访问
- [ ] 获得100个注册用户
- [ ] 至少1个付费用户

---

## 七、风险与应对

### 7.1 技术风险

**风险1: 准确率达不到预期** 🔴
- 概率: 30%
- 影响: 用户不愿付费
- 应对:
  - 多轮测试和优化
  - 降低预期,明确告知限制
  - 收集数据持续改进

**风险2: 成本失控** 🟡
- 概率: 20%
- 影响: 入不敷出
- 应对:
  - 严格限流(免费版)
  - 监控API使用
  - 设定月度预算上限($200)

**风险3: 性能问题** 🟡
- 概率: 25%
- 影响: 用户体验差
- 应对:
  - 异步处理
  - 缓存常见问题
  - 使用Haiku处理简单问题

### 7.2 市场风险

**风险1: 无人使用** 🟡
- 概率: 40%
- 影响: 项目失败
- 应对:
  - 这本来就是学习项目
  - 重点是掌握技术
  - 可以转向其他方向

**风险2: 竞品降价** 🟡
- 概率: 30%
- 影响: 价格优势消失
- 应对:
  - 差异化(更好准确率、中文优化)
  - 提供更好的用户体验
  - 快速迭代新功能

**风险3: ChatGPT添加类似功能** 🔴
- 概率: 50% (未来1年)
- 影响: 市场消失
- 应对:
  - 尽快获取用户
  - 建立品牌和用户粘性
  - 做垂直优化(如学术论文专用)

### 7.3 个人风险

**风险1: 时间不够** 🟡
- 概率: 35%
- 影响: 延期或质量下降
- 应对:
  - MVP砍功能
  - 先做Streamlit(更快)
  - 允许延期1-2周

**风险2: 技术遇阻** 🟡
- 概率: 30%
- 影响: 卡住进度
- 应对:
  - 充分利用Claude Code
  - 搜索解决方案
  - 简化实现方式

---

## 八、成功标准

### 8.1 最低标准(必须达到)

- [ ] 产品能正常工作,无严重bug
- [ ] 准确率>80%,有来源引用
- [ ] 部署上线,可公开访问
- [ ] 获得10+真实用户试用

### 8.2 理想标准

- [ ] 准确率>85%
- [ ] 100+注册用户
- [ ] 5+付费用户
- [ ] D7留存>30%
- [ ] 月收入>$50

### 8.3 加分标准

- [ ] Product Hunt Daily Top 10
- [ ] 500+用户
- [ ] 20+付费用户
- [ ] 月收入>$200
- [ ] 有用户主动分享推荐

---

## 九、后期扩展方向

**如果MVP成功,可以考虑**:

### Phase 2功能(3-6个月后)
- [ ] OCR支持(扫描件)
- [ ] 多PDF对比
- [ ] 导出对话为Markdown
- [ ] 生成文档摘要
- [ ] Chrome扩展

### Phase 3功能(6-12个月后)
- [ ] 移动App
- [ ] 团队协作
- [ ] API开放
- [ ] 企业版
- [ ] 多语言支持

**但现在不要想这些,专注MVP!**

---

## 十、总结

### 项目价值评估

**学习价值**: ⭐⭐⭐⭐⭐
- 完整的RAG系统实践
- LangChain深度应用
- 全栈产品开发
- 商业化运营经验

**简历价值**: ⭐⭐⭐⭐⭐
- "开发并发布AI SaaS产品"
- "实现RAG系统,准确率85%+"
- "获得X用户,X付费,月收入$X"
- 技术栈完整,可展示

**商业价值**: ⭐⭐⭐
- 不一定赚大钱
- 但6个月内月入$100-500很有可能
- 验证了创业能力

### 核心原则

1. **质量第一**: 准确率>功能数量
2. **快速验证**: 3周MVP,不要拖延
3. **用户导向**: 收集反馈,持续优化
4. **务实态度**: 这是学习项目,不是all-in创业
5. **享受过程**: 做出真正有用的产品很酷!

---

**下一步**: 创建详细的实施计划(week-by-week任务分解)

---

**文档版本**: v1.0
**最后更新**: 2026-02-24
